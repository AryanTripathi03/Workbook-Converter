# Workbook-Converter
FastAPI backend to securely log in and merge multiple Excel (.xlsx, .xls) and CSV files into a single workbook. Supports multiple users, CORS-enabled, and streams the merged file directly without saving on the server.  Tech: Python 3.11+, FastAPI, Pandas, Openpyxl Endpoints: /api/login/ and /api/upload/
